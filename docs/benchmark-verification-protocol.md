# Benchmark Verification Protocol

* how benchmark results are generated
* how corpus version is pinned
* how result hashing works
* how verification works
* reproducibility commands
* accepted artifacts
* what invalidates a result
* limitations
